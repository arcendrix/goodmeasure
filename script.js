const revealItems=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});revealItems.forEach(item=>observer.observe(item));const counters=document.querySelectorAll('[data-count]');const counterObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target;const target=Number(el.dataset.count);let current=0;const step=Math.max(1,Math.ceil(target/42));const timer=setInterval(()=>{current+=step;if(current>=target){current=target;clearInterval(timer)}el.textContent=current},24);counterObserver.unobserve(el)})},{threshold:.5});counters.forEach(counter=>counterObserver.observe(counter));const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{nav.classList.toggle('open');menuButton.classList.toggle('open')})}const compass=document.querySelector('[data-compass]');if(compass){const inputs=[...compass.querySelectorAll('input[type=range]')];const score=compass.querySelector('[data-score]');const bars=[...compass.querySelectorAll('[data-bar]')];const note=compass.querySelector('[data-note]');function updateCompass(){let total=0;inputs.forEach((input,i)=>{const value=Number(input.value);total+=value;if(bars[i])bars[i].style.height=`${value*10}%`;const valueLabel=input.closest('.assessment-row')?.querySelector('strong');if(valueLabel)valueLabel.textContent=value});const avg=Math.round((total/(inputs.length*10))*100);if(score)score.textContent=avg;if(note){note.textContent=avg<45?'Starting point: build structure before pressure.':avg<70?'Developing: the foundation is there, but consistency needs work.':'Strong foundation: focus on leadership, contribution, and next-level responsibility.'}}inputs.forEach(input=>input.addEventListener('input',updateCompass));updateCompass()}